#include <iostream>
#include <stdio.h>
#include <cmath>
#include <memory.h>
#include <algorithm>
#define maxn 100000+100

using namespace std;
int n,denta,a[maxn];
int tim( int x )
{
    int l=1,r=n,res=0;
    while ( l<=r ){
        int mid;
        mid=( l+r ) / 2;
        if ( a[mid]<                  )
    }
}
int main()
{   freopen("TRIP.inp","r",stdin);
    freopen("TRIP.out","w",stdout);
    cin>>n>>denta;
    for (int i=1; i<=n; i++) cin>>a[i];

}
