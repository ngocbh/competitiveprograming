#include <bits/stdc++.h>

const long N = 10000
using namespace std;
int n, k, A, B, a_size;
int a[N], f[N][N]
int main()
{
    freopen("VMROPES.inp","r",stdin);
    cin >> n >> k >> A >> B;
}
