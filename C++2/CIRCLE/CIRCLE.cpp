#include <bits/stdc++.h>

const long N = 41;
using namespace std;
int n;
int main()
{   freopen("CIRCLE.inp","r",stdin);
    cin >> n;

    for (int i = 1; i <= n; i++)
            for (int j = 1; j <= n; j++)  {
                    ok = true;
                    for (int k = 1; k <= n; k++) {
                        if ( i >= k ) {
                            if ( )
                        }
                    }
            }

}
