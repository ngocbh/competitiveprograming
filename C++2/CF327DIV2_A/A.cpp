#include <bits/stdc++.h>

using namespace std;

int main()
{   //freopen("A.inp","r",stdin);
    double l, p , q;
    cin >> l >> p >> q;

    double ans = ( l / (p+q) ) * p;
    cout << ans;
}
