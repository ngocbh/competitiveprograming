#include <iostream>
#include <stdio.h>
#include <memory.h>
#include <cmath>
#define maxn 10000+100
#define 30000+100
#define X first
#define Y second
using namespace std;
int n,ax[2*maxn],ay[2*maxn];
pair <int,int> tt[maxn],pd[maxn];
int main()
{   freopen("area.inp","r",stdin);
    freopen("area.out","w",stdout);
    scanf("%d",&n);
    for (int i=1; i<=n; i++)

}
